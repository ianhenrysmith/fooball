%nav.navbar.navbar-inverse.navbar-static-top{ role: 'navigation'}
  .container-fluid
    .navbar-header
      = link_to "fooball", root_path, class: 'navbar-brand'

    %ul.nav.navbar-nav
      - if current_user
        %li= link_to 'Your Profile', user_url(current_user)

      - else
        %li.btn.btn-info= link_to "Sign in with Google", user_omniauth_authorize_path(:google_oauth2)